services:
  web:
    root_dir: .
    build_command: |
      cd frontend
      npm install
      npm run build
      echo "📂 Checking build output:"
      ls -la dist/
      cd ..
      echo "📂 Root structure:"
      ls -la
    start_command: cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT
